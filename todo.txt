canvas renderer
gui system
camera lookat 
make text positioning scale with window
make function to set global sound volume
writing to files

skybox rendering

function timing
use mapping to update constant buffers
handle updating resolution on window resize

import fonts from .ttf .otf
optimize input
improve lighting
replace math.h's trig functions
animated meshes
look into getting rid of padding variables in constant buffer structs
